# packages

Shared TypeScript libraries (ui, api-client, types, validation, config, plugin-sdk).
Not populated by the Platform Core bootstrap — Platform Core is backend-only
(services/platform). See docs/architecture/monorepo-bootstrap.md.
